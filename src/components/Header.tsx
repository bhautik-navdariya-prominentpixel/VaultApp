// import { useEffect, useState } from "react";

const Header = () => {
  // const [darkMode, setDarkMode] = useState(false);
  
  // useEffect(() => {
  //   if (darkMode) {
  //     document.documentElement.classList.add("dark");
  //   } else {
  //     document.documentElement.classList.remove("dark");
  //   }
  // }, [darkMode]);
  return (<></>
    // <header className="w-full border-b border-gray-300 dark:border-gray-700 bg-white dark:bg-gray-800 shadow-sm mb-2">
    //     <div className="max-w-7xl mx-auto px-6 py-4 flex items-center justify-between">
    //       {/* Logo / Brand Name */}
    //       <h1 className="text-2xl font-bold text-slate-800 dark:text-slate-300">
    //         VaultPay
    //       </h1>

    //       {/* Theme Toggle Button */}
    //       <button
    //         onClick={() => setDarkMode(!darkMode)}
    //         className="flex items-center space-x-2 px-3 py-2 rounded-lg border border-gray-300 dark:border-gray-600 bg-gray-100 dark:bg-gray-700 hover:scale-105 transition"
    //       >
    //         {darkMode ? "🌞 Light" : "🌙 Dark"}
    //       </button>
    //     </div>
    //   </header>
  );
};

export default Header;
